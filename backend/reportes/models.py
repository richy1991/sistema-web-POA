from django.db import models

# The 'reportes' app might not need models if it only generates reports
# from data in other apps. If you have tables specific to report
# generation (e.g., saved report templates), you can define them here.
